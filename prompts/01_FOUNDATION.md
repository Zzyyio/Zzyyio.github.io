# Phase 1 提示词：骨架与视觉令牌

完成 `docs/PHASES.md` 的 Phase 1。建立三个可直达路由、固定导航、页面元数据、favicon 和全局视觉令牌。使用暖白纸面、黑色文字、发丝边框和大留白。主页第一视口必须直接呈现身份与定位，不能出现通用模板文案。

保持语义 HTML、跳到主要内容链接、当前页 `aria-current` 和可见键盘焦点。不要实现复杂波纹物理；只为持久视觉层预留结构。

验收时直接打开三个路由，检查移动端无横向滚动，并运行构建。
