# Phase 5 提示词：响应式、可访问性与性能

完成 `docs/PHASES.md` 的 Phase 5。检查 375×812、768×1024、1440×900 三种视口，以及 200% 文字缩放。

保证导航、跳转链接、项目锚点、邮箱链接可用且有焦点状态。确保背景线、Canvas 和照片不会制造横向滚动。摄影网格在手机上改为有节奏的单列错位，不压缩成微小缩略图。

模拟 WebGL2 初始化失败，确认内容和 SVG 黑线仍正常。开启减少动态效果，确认连续线束波纹和回声线停止。分析动画循环的对象分配、线束上限、清理逻辑和 DPR。

只修复实际问题，不为了测试而重写架构。完成后运行构建与 lint，并记录仍需真实设备验证的项目。
